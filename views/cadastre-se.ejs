<!DOCTYPE html>
<html lang="pt-Br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/login.css">
</head>
<body>
    <div class="container">
        <div class="text-center">
            <h1>Cadastre-se</h1>
        </div>
        <form method="post" action="/cadastraUsuario">
            <div class="md-4">
                <label for="campoTexto" class="form-label">Email</label>
                <input type="text" class="form-control" id="campoTexto" name="user" placeholder="Digite algo..."required>
            </div>
            <div class="md-4">
                <label for="campoNumber" class="form-label">Senha</label>
                <input type="password" class="form-control" id="campoNumber" name="password" placeholder="Digite algo..."required>
            </div>
            <div class="md-4">
                <label for="campoNumber" class="form-label">Repita sua senha</label>
                <input type="password" class="form-control" id="campoNumber" name="repeatpassword" placeholder="Digite algo..." required>
            </div>
            <div id="btn" class="text-center">
                <br>
                <button class="btn btn-primary">Cadastrar</button>
            </div>
        </form>
        <% if( alertUser==true) { %>
            <script>
                alert("Usuario ja cadastrado")
            </script>

            <% }else {%>

            <% if( alertPass==true) { %>
                <script>
                    alert("Senhas não Coicidem")
                </script>
            <% }} %>   
    </div>
</body>
<%- include('./partials/footer.ejs') %>